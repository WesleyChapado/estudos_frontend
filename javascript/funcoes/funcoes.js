// exemplo de uma função
function soma(valor1, valor2) {
    // Criando uma variável dentro do escopo da função usando let
    let resultado = valor1 + valor2;
    return resultado;
}

// Exemplo de uso da função soma
console.log(soma(5, 3)); // Saída: 8